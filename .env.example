# ローカル開発用。このファイルをコピーして .env を作成し、実際の値を入れてください。
# .env は .gitignore 対象なのでコミットされません。

GEMINI_API_KEY=
ANTHROPIC_API_KEY=
SIMILARITY_THRESHOLD=0.65
SIMILARITY_CLARIFY_THRESHOLD=0.6

# チャットログ記録用（既存Supabaseプロジェクトのanon keyを使用。service_role keyは使わない）
SUPABASE_URL=
SUPABASE_ANON_KEY=
