---
/**
 * サイト全体の水平リズム。ヘッダー・フッター・各セクションで同一の max-width / padding。
 */
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-[var(--page-max)] px-6', className]}>
  <slot />
</div>
